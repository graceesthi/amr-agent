[pytest]
# src/ on the path so tests can import the agent modules by their bare names,
# which is how src/ imports them internally too.
pythonpath = src
testpaths = tests
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
